name: Deploy to NAS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull and deploy
        run: |
          cd /nas-project
          git pull origin main
          docker compose up -d --build lineoa
          docker image prune -f
